# Copy to .env and adjust as needed.
# SQLite file used by the backend (auth, saved sessions, leaderboard).
# For a hosted deployment switch the provider in prisma/schema.prisma to
# "postgresql" and point this at your Postgres instance.
DATABASE_URL="file:./prisma/dev.db"
