# Autopilot — loops oracfit — 2026-08-12 (tarde)

> Boss: "pode ir desenvolvendo você mesmo, use a metodologia do oracfit de
> loops e barras, escreva/avalie/audite tudo em subagents. Quando eu voltar
> quero ver onde parou e por que parou."
>
> Protocolo por loop: eu (orquestrador) especifico → builder subagent
> implementa com testes → reviewer subagent audita o diff → eu corrijo/integro
> → gates (tsc + suite + build) → commit. Um loop por vez, sem paralelismo de
> escrita no working tree.

## Fila (ordem da party 2026-08-12 + decisões do Boss)

| # | Loop | Status |
|---|---|---|
| 1 | 💎 por posição no ranking de fim de run (PRD §3.2) | `[██████████]` ✅ commitado |
| 2 | Missões básicas (3 diárias, server-authoritative) | `[██████████]` ✅ commitado |
| 3 | E2E Playwright do fluxo called shot | `[██████████]` ✅ commitado |
| 4 | Fundação i18n (interface própria, EN+PT-BR) | `[██████████]` ✅ commitado |
| 5 | Eventos de run (Volatilidade Extrema, fatia atmosférica) | `[██████████]` ✅ commitado |

## Log de execução

- 14:50 — fila montada, especificações em rascunho. Loop 1 despachado.
- 15:0x — Loop 1: builder entregou (818 testes). Auditoria achou **exploit
  crítico**: replay do POST /api/sessions cunhava diamantes (payload forjado →
  rank 1 → +30/chamada). Correção: cooldown de 5 min server-side + bounds no
  returnPercent (-100..10000) + try/catch no award + token de staleness no
  hook + Math.max no espelho local. 827 testes, tsc e build limpos. Commit.
- 15:3x — Loop 2: missões diárias (Close the Day 10💎 / Sniper 15💎 / In the
  Green 12💎), model MissionClaim + migração, claim server-authoritative
  (400/409/422), aba Missions do shell mobile deixou de ser "coming soon".
  Auditoria: 4 riscos médios — claim/crédito compensável (rollback do claim se
  addDiamonds falhar), guest vs erro separados no client (CTA vs Retry),
  refetch+erro inline em claim falho; faucet de sessões forjadas aceito como
  dívida R2 documentada (~22💎/dia máx). 877 testes. Commit.
- 16:1x — Loop 3: e2e/called-shot.spec.ts com 4 cenários (declaração, voided,
  hit, missed) + conserto real no harness (mock-binance mirava host que o app
  nunca chama — era no-op silencioso; e reload duplo em dev apagava a store).
  Auditoria pegou pegadinha fina: candles 100% flat disparavam o detector de
  dados degradados e o app trocava o mock por random-walk — determinismo era
  ilusão. Corrigido (high/low ±0.01), data-testids contra fragilidade de i18n,
  probe-antes-do-banner. Stress 20/20 verde; suíte e2e completa 43 passed.
  Commit.
- 16:4x — Loop 4: fundação i18n SEM lib externa (pivô do orquestrador: provider
  do next-intl quebraria dezenas de testes RTL; regra AGENTS de interface
  própria) — catálogos tipados EN/PT-BR (paridade estrutural via tsc + teste de
  sentinela contra interpolação descartada), gameLocale persistido, hook
  useGameMessages, 8 superfícies roguelike migradas, LocaleToggle no Header
  com gate de mounted contra mismatch de hidratação. Smoke PT e called-shot
  e2e verdes. 902 testes. Commit.

## Loop 5 — desbloqueado e entregue (17:11–17:4x)

Boss escolheu a opção (a) — fatia atmosférica. Entregue: detecção
determinística de janelas reais de volatilidade no buffer da run (máx. 2,
congeladas por run — auditoria pegou teleporte de janelas em buffer estendido,
banner preso em "1s left" com dados exauridos, flicker de fronteira em
pause/resume, e dados sintéticos do fallback alimentando o detector — tudo
corrigido, incl. flag `isSynthetic` plumbado no binance-api), countdown de 10s
+ banner ativo de 45s, copy i18n EN/PT-BR prometendo "movimentos maiores"
(nunca recompensa multiplicada). Multiplicador oficial segue dívida R2.
936 testes.

---

# Sessão 2 — godmode v4 (18:15) — eliminar dívida técnica

| # | Loop | Status |
|---|---|---|
| A | Rate limiting nas rotas de mutação (auth/calls/sessions/missions) | `[██████████]` ✅ commitado — auditoria pegou bypass por spoof de XFF (1º hop é do cliente; corrigido p/ último hop + x-real-ip + request.ip), pré-filtro por IP antes do auth, sweep do Map, callMutations 60/min |
| B | i18n: extração das telas legadas de trading + missões traduzíveis | `[██████████]` ✅ commitado — ~120 chaves, 20+ componentes, validações TP/SL injetáveis, missões traduzidas c/ fallback + tripwire; EN byte-idêntico (auditoria confirmou); restante: LandingPage, Achievements/Ranking/Leaderboard. Descoberta: 8 falhas e2e PRÉ-existentes (specs dependem de preço vivo) → alimenta Loop D |
| C | tradingStore.test.ts (1231 linhas) → dividir sob o limite de 500 | `[██████████]` ✅ commitado — split verbatim em 5 arquivos (61–406 linhas), 67/67 testes preservados (diff de títulos + multiset de linhas), auditoria: clean bill (reset global do afterEach anula resíduo entre arquivos) |
| D | e2e: suíte estava 27 failed em silêncio → **0 failed** (50 passed/8 skipped, 2 rodadas); helpers sem sleeps cegos; 56 sleeps eliminados nos specs tocados (~87 restam em specs verdes = dívida menor) | `[██████████]` ✅ commitado |
| E | **Bug de produto achado pelo D2**: refactor 3193d78 perdeu a UI do trailing stop (setTrailingStop órfão; 4 e2e parkados como fixme) → restaurar UI + reviver testes | `[███░░░░░░░]` em execução (substituiu Postgres na fila) |
| F | Prontidão Postgres (validar troca de provider com Docker) | `[░░░░░░░░░░]` stretch, provavelmente fica p/ próxima sessão |
| — | OAuth (GitHub/Google/X) | bloqueado: exige credenciais de app que só o Boss cria |

## Registro histórico da parada anterior (17:00, superado às 17:11)

**Parou no Loop 5 (Eventos de run — Volatilidade Extrema), antes de escrever
código.** Quatro de cinco loops entregues e pushados (`7ff6330`, `6ed413f`,
`ab6e018`, `70397e8`). Baseline: 799 → **902 testes** (+103), e2e 39 → 43
specs, tsc/build limpos em todos os commits.

Por que parou — análise do Loop 5 (PRD §3.3):

1. O efeito do evento é "multiplicador de recompensa de calls" e recompensa é
   **server-authoritative por lei da casa**. Só que o servidor não conhece as
   candles do run — o cliente escolhe a janela histórica e busca via proxy.
   Qualquer flag "call declarado durante o evento" viria do cliente →
   **forjável**, exatamente a classe de exploit que a auditoria matou no
   Loop 1 (replay de sessões cunhando 💎). Fechar isso exige o modelo `Run`
   server-side com seed/janela conhecida — dívida R2 já registrada (PRD §9.2).
2. Existe fatia v1 sem economia: detectar volatilidade nas candles reais já
   carregadas (sem sintetizar preço, PRD respeitado), anunciar com countdown e
   destacar por ~45s — só atmosfera, sem multiplicador. É uma **redução de
   escopo que muda o valor da feature → decisão de produto do Boss**, não do
   autopilot.
3. A curadoria de janelas históricas (bottleneck apontado na party, dono:
   Mary) continua sendo o caminho para "eventos idênticos para todos" quando a
   R2 trouxer seed compartilhada.

Opções na mesa para o Boss: (a) fatia atmosférica agora, multiplicador na R2;
(b) esperar o modelo Run server-side e entregar completo; (c) aceitar
multiplicador forjável temporariamente (desaconselhado — reabre a porta que a
auditoria fechou).
